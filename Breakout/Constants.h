#pragma once

const float LEFT_EDGE = -4.05f;
const float RIGHT_EDGE = 4.05f;
const float TOP_EDGE = 3.f;
const float EDGE_SIZE = 0.1f;

const Vector3 PADDLE_SIZE = Vector3(1.f, 0.15f, 0.2f);
const Vector3 BALL_SIZE = Vector3(0.15f, 0.15f, 0.15f);
